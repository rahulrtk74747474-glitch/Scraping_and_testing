name: NIFTY Order Block 15m paper trader

on:
  schedule:
    - cron: "2,17,32,47 * * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".ob-nifty-trigger"

permissions:
  contents: write

concurrency:
  group: ob_nifty-15m-paper
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout isolated paper branch
        uses: actions/checkout@v4
        with:
          ref: ob-nifty-15m-paper
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Run 15m order-block paper trader
        run: python -m src.order_block_paper

      - name: Commit state and reports to ob-nifty-15m-paper only
        run: |
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name "ob-nifty-paper-bot"
          git config user.email "ob-nifty-paper-bot@users.noreply.github.com"
          git add data/ob_nifty_*.csv data/ob_nifty_state.json reports/ob_nifty_latest.md
          git commit -m "chore: ob_nifty 15m order-block paper update"
          git push origin HEAD:ob-nifty-15m-paper
