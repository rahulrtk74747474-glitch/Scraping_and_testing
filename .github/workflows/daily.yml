name: Vertex 3:15 PM paper trade

on:
  workflow_dispatch:
  push:
    branches: [vertex-315-paper]
    paths:
      - ".vertex315-trigger"

permissions:
  contents: write

concurrency:
  group: vertex-315-paper-trade
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Python dependencies
        run: pip install -r requirements.txt
      - name: Install Chromium for Chartink scraping
        run: python -m playwright install --with-deps chromium
      - name: Run daily engine
        env:
          CHARTINK_SCAN_URL: https://chartink.com/screener/vertex-53
          CHARTINK_USER: ${{ secrets.CHARTINK_USER }}
          CHARTINK_PASSWORD: ${{ secrets.CHARTINK_PASSWORD }}
          CHARTINK_COOKIE: ${{ secrets.CHARTINK_COOKIE }}
        run: python -m src.main
      - name: Commit daily state and reports
        run: |
          if git diff --quiet && git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name "vertex-paper-bot"
          git config user.email "vertex-paper-bot@users.noreply.github.com"
          git add data reports
          git commit -m "chore: 3:15 PM paper-trading update"
          git push
